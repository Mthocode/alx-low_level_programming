#include <stdio.h>
/**
 * main - main block
 * Description: display _putchar
 * Return: return 0 (success)
 */

int main(void)
{
	char unScore = '_';
	char p = 'p';
	char u = 'u';
	char t = 't';
	char c = 'c';
	char h = 'h';
	char a = 'a';
	char r = 'r';

	putchar(unScore);
	putchar(p);
	putchar(u);
	putchar(t);
	putchar(c);
	putchar(h);
	putchar(a);
	putchar(r);

	return 0;
}

