#include "main.h"
/**
 * add -  adds two integers and returns the result
 * @a: firs integer to be add
 * @b: second integer to be add
 * Return: returns result
 */
int add(int a, int b)
{
	int result = a = b;

	return (result);
}
